# API reference

This reference explains a fictional API surface used by manual samples.

## `create_profile(user_id, display_name)`

Creates a profile object for the sample domain.

## `search(query)`

Returns matching sample document identifiers.
