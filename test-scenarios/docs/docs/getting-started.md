# Getting started

This guide documents the manual validation flow for the scoring classifier.

## Steps

1. Stage only the files for one expected outcome.
2. Run `git-acp --dry-run`.
3. Confirm the generated commit type matches the expected classification.

The document is intentionally docs-only and should classify as documentation.
