# Rollback note

This file is a manual sample for revert-oriented validation.

Revert classification is intentionally driven by the commit message prefix, not
by file paths. Stage this file with the rest of the revert scenario and use a
message such as `revert: restore previous sample behavior`.
